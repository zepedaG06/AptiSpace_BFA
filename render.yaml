services:
  - type: web
    name: aptispace-bfa
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /AptiSpace_BFA/m/Usuario
    envVars:
      - key: PORT
        value: 8080
      - key: JDBC_DATABASE_URL
        fromDatabase:
          name: aptispace-db
          property: connectionString
      - key: APTISPACE_SESSION_SECRET
        generateValue: true

databases:
  - name: aptispace-db
    plan: starter
